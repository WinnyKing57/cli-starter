# {{project_name}} - Fullstack

This is a fullstack project with a React frontend and an Express backend.

## Structure

- `/client`: React frontend
- `/server`: Express backend

## Getting Started

Run `npm install` in the root, then `npm run dev` to start both projects.
